# user.moph — 人

import { arc } from shape

## 人。頭と肩。誰か、持ち主に
## @category もの
## @type {List<List<Vector>>} 0..1 の箱に収めた輪郭の並び
export let USER = [
  arc(0.5, 0.25, 0.2, 0, 360, 20),
  # 肩。半円の弦がそのまま下の縁になる
  arc(0.5, 0.97, 0.36, 90, -90, 18),
]
