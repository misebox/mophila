# statement.moph — 制御構文 (すべて仮)

let n = 5

# if / else
if n > 3 {
  log("big")
} else if n > 1 {
  log("mid")
} else {
  log("small")
}

# for: 範囲
for i in 0..5 {          # 0, 1, 2, 3, 4
  log(i)
}
for i in 0..=5 {         # 0 〜 5
  log(i)
}

# for: リスト
let colors = [#e04040, #40a040, #4040e0]
for col in colors {
  let c = Circle(fill = col)
}

# for: インデックス付き
for (i, col) in colors.enumerate() {
  log("{i}: {col}".format(i, col))
}

# while: 回数が先に決まらないとき。100 万回で止まらなければエラーになる
let total = 0
let steps = 0
while total < 5 {
  total = total + 0.7
  steps = steps + 1
}
log("{steps} 歩で {total}".format(steps, total))

# break: for も while もその場で抜ける (continue は無い)
for i in 0..100 {
  if i * i > 30 { break }
}

# ブロックは値を返す (最後の式)
let size = if n > 3 { 1/2 } else { 1/4 }
